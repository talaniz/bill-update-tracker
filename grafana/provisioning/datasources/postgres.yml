apiVersion: 1

datasources:
  - name: Bill Update Tracker Postgres
    type: postgres
    access: proxy
    uid: bill-update-postgres
    url: postgres:5432
    database: bill_update_tracker
    user: tracker
    secureJsonData:
      password: tracker
    jsonData:
      sslmode: disable
      postgresVersion: 1600
      timescaledb: false

